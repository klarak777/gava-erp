services:
  db:
    image: pgvector/pgvector:pg16
    container_name: gava_erp_db
    restart: always
    environment:
      POSTGRES_DB: gava_erp
      POSTGRES_USER: gava_admin
      POSTGRES_PASSWORD: adminpassword123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
